% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tabulate.R
\name{stata_tabulate}
\alias{stata_tabulate}
\title{Stata-Style Tabulate function}
\usage{
stata_tabulate(df)
}
\arguments{
\item{df}{A data frame containing the categorical variables to tabulate.}
}
\value{
Prints formatted frequency tables to the console. The function does not return a value.
}
\description{
Provides an interactive console in R that mimics Stata's \code{tabulate} command.
Users can type commands at the \verb{StataR>} prompt to create frequency tables
for categorical variables, optionally summarizing by a grouping variable.
The output includes frequencies, percentages, cumulative percentages, and totals.
}
\examples{
\dontrun{
# Start the interactive StataR tabulate console
stata_tabulate(ggplot2::diamonds)

# In the console, example commands:
 tabulate cut
 tabulate cut, by(color)
 exit
}
}
